# Socket.io Server URL
VITE_SOCKET_URL=http://localhost:5000

# Backend API URL (optional, defaults to current domain)
# Used for API requests - leave empty for relative URLs via proxy
VITE_API_URL=http://localhost:5000
